const express = require('express');
const {} = require('./constants');

const app = express();

//Import routes

//Configure Body Parse

//Configure Header HTTP - CORS

//Configure routings

module.exports = app;